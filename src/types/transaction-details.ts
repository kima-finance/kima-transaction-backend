export interface TransactionDetails {
  amount: number
  fee: number
  originAddress: string
  originChain: string
  originSymbol: string
  targetAddress: string
  targetChain: string
  targetSymbol: string
}
